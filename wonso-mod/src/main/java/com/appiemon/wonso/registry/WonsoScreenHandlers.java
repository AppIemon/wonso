package com.appiemon.wonso.registry;

/**
 * Menu types for synthesis / refining screens.
 * v1 uses packet-driven GUIs (client screens + server validation) so
 * container menus can stay thin until slot layouts match elementwar.
 */
public final class WonsoScreenHandlers {
	private WonsoScreenHandlers() {
	}

	public static void initialize() {
	}
}
